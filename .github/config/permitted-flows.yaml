defaults:
  flows:
    - install
    - upgrade-patch
    - upgrade-minor
    - modular-upgrade-minor
rules:
  - match: "<=8.7"
    deny:
      - upgrade-minor
  - match: "==8.10"
    deny:
      - upgrade-patch

